import AdminPlaceholderPage from './AdminPlaceholderPage';

export default function EmployeesPage() {
  return <AdminPlaceholderPage title="Employees" description="Employee directory with BambooHR sync and manual overrides." />;
}
