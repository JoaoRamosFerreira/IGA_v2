import PagePlaceholder from '../components/PagePlaceholder';

export default function AssetsPage() {
  return (
    <PagePlaceholder
      title="Assets"
      description="Connected applications, login methods, and privileged group metadata will be managed here."
    />
  );
}
