import AdminPlaceholderPage from './AdminPlaceholderPage';

export default function SettingsPage() {
  return <AdminPlaceholderPage title="Settings" description="Admin settings for revocation, retention, purge, and integration controls." />;
}
